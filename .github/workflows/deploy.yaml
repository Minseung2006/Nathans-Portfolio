on: workflow_dispatch

deploy:
  runs-on: ubuntu-latest
  steps:
    - uses: actions/checkout@v4
    - uses: actions/setup-dotnet@v4
      with:
        cache: true
    - run: dotnet build Portfolio.sln
